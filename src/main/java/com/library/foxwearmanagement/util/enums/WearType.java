package com.library.foxwearmanagement.util.enums;

public enum WearType {
    TOP,         // ? Üst geyimləri
    BOTTOM,      // ? Alt geyimləri (şalvar)
    DRESS,       // ? Paltarlar
    OUTERWEAR,   // ? Çöl geyimləri (palto, kaban)
    FOOTWEAR,    // ? Ayaqqabılar
    ACCESSORY,   // ? Aksesuarlar
    UNDERWEAR,   // ? Alt geyimləri
    SWIMWEAR     // ? Çimərlik geyimləri
}
